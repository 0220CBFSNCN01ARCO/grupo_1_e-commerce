<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/style2.css">
    <link rel="stylesheet" href="/css/formulario.css">
    <title>Nexus</title>
    <%- include('./partials/head.ejs') %>

</head>

<body>
    <%- include('./partials/header.ejs') %>

        <div class="container">
           
            <div class="formulario">
                <form action="/users/register" method="POST" enctype="multipart/form-data">
                    <h1 class="titulo-formulario">Registrate</h1>
                    <div class="form-group">
                        <label for="exampleInputEmail1" class="nombre">Nombre</label>
                        <input type="text" class="form-control" id="inputsFormulario" aria-describedby="emailHelp" placeholder="Nombre" name="fname">
                        <small id="mensajeDebajoDeInput" class="form-text text-muted">
                            <% if (typeof errores != 'undefined') { %>
                                <p style="color: red">
                                    <% for( let i = 0; i < errores.length; i++ ) { %>
                                        <% if (errores[i].param == 'fname') { %>
                                            <%= errores[i].msg %> 
                                        <% } %>
                                <% } %>
                                 </p>
                                
                            <% } else { %>
                                Tus datos estan seguros con nostros
                            <% } %>
                        </small>
                        <label for="exampleInputEmail1" class="nombre">Apellido</label>
                        <input type="text" class="form-control" id="inputsFormulario" aria-describedby="emailHelp" placeholder="apellido" name="lname">
                        <small id="mensajeDebajoDeInput" class="form-text text-muted">
                            <% if (typeof errores != 'undefined') { %>
                                <p style="color: red">
                                    <% for( let i = 0; i < errores.length; i++ ) { %>
                                        <% if (errores[i].param == 'lname') { %>
                                            <%= errores[i].msg %> 
                                        <% } %>
                                <% } %>
                                 </p>
                                
                            <% } else { %>
                                Tus datos estan seguros con nostros
                            <% } %>
                        </small>

                        <label for="exampleInputEmail1" class="correoElectronico">Correo electronico</label>
                        <input type="email" class="form-control" id="inputsFormulario" aria-describedby="emailHelp" placeholder="Correo electronico" name="email">
                        <small id="mensajeDebajoDeInput" class="form-text text-muted">
                            <% if (typeof errores != 'undefined') { %>
                                <p style="color: red">
                                    <% for( let i = 0; i < errores.length; i++ ) { %>
                                        <% if (errores[i].param == 'email') { %>
                                            <%= errores[i].msg %> 
                                        <% } %>
                                <% } %>
                                 </p>
                                
                            <% } else { %>
                               Nunca compartiremos tu correo con nadie
                            <% } %>
                        </small>

                        <label for="exampleInputEmail1" class="correoElectronico">Correo electronico</label>
                        <input type="email" class="form-control" id="inputsFormulario" aria-describedby="emailHelp" placeholder="Confirmar correo" name = 'emailConfirm'>
                        <small id="mensajeDebajoDeInput" class="form-text text-muted">
                            <% if (typeof errores != 'undefined') { %>
                                <p style="color: red">
                                    <% for( let i = 0; i < errores.length; i++ ) { %>
                                        <% if (errores[i].param == 'emailConfirm') { %>
                                            <%= errores[i].msg %> 
                                        <% } %>
                                <% } %>
                                 </p>
                                
                            <% } %>
                        </small>
                    </div>
                    <div class="form-group">
                        <label for="exampleInputPassword1" id="contraseña" placeholder="Contraseña">Contraseña</label>
                        <input type="password" class="form-control" id="inputsFormulario" placeholder="Contraseña" name="pass">
                        <small id="mensajeDebajoDeInput" class="form-text text-muted">
                            <% if (typeof errores != 'undefined') { %>
                                <p style="color: red">
                                    <% for( let i = 0; i < errores.length; i++ ) { %>
                                        <% if (errores[i].param == 'pass') { %>
                                            <%= errores[i].msg %> 
                                        <% } %>
                                <% } %>
                                 </p>
                                
                            <% } %>
                        </small>
                    </div>
                    <div class="form-group">
                        <label for="exampleInputPassword1" id="contraseña" placeholder="Contraseña">Foto de perfil</label>
                        <input type="file" id="inputsFormulario" aria-describedby="emailHelp" name="user">
                    </div>
                    <button type="submit" class="btn btn-dark" id="crear-cuenta">Crear cuenta</button>
                    <div class="iconos">
                        <i class="fa fa-instagram" aria-hidden="true"></i>
                        <i class="fa fa-twitter-square" aria-hidden="true"></i>
                        <i class="fa fa-youtube" aria-hidden="true"></i>
                        <i class="fa fa-envelope-o" aria-hidden="true"></i>



                    </div>
                </form>
            </div>
            <button type="submit" class="btn btn-dark" id="ingresar">Ingresar</button>

        </div>

        <%- include('./partials/footer.ejs') %>
</body>

</html>